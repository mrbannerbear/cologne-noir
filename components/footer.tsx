export function Footer() {
  return (
    <footer className="relative z-10 border-t border-white/8 bg-black/50">
      <div className="mx-auto grid w-full max-w-7xl gap-4 px-4 py-8 text-sm text-muted sm:px-6 lg:grid-cols-2 lg:px-8">
        <p>
          Chittagong-based perfume decants and full bottles. COD only — every order is confirmed
          by WhatsApp before shipping across Bangladesh.
        </p>
        <p className="lg:text-right">Follow @cologne.noir on Instagram for new drops.</p>
      </div>
    </footer>
  );
}
